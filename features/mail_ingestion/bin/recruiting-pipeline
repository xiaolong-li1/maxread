#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
cd "$ROOT"
export PYTHONPATH="$ROOT/features/mail_ingestion/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m recruiting_pipeline "$@"
